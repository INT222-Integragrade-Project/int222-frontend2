version: '3.3'
services:
  front-docker:
    image: ghcr.io/artidsun/int222-frontend:latest
    container_name: frontend
    ports:
      - "8080:8080"
      - "443:443"
networks:
    default:
      external:
        name: project2-net