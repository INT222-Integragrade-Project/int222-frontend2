version: '3.3'
services:
  front-docker:
    image: ghcr.io/artidsun/int222-frontend-uat:latest
    container_name: frontend-dev
    ports:
      - "8080:8080"
networks:
    default:
      external:
        name: project2-net  docker pull